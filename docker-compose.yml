services:
  db:
    image: postgres:15
    container_name: postgres_billing_prod
    restart: unless-stopped
    volumes:
      - /mnt/ebs/postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - billing_network

  backend:
    build: .
    container_name: billing-backend
    restart: unless-stopped
    ports:
      - "8002:8000"
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    volumes:
      - /mnt/ebs/media:/app/media
      - /mnt/ebs/staticfiles:/app/staticfiles
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/billing/health/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - billing_network

  nginx:
    image: nginx:latest
    container_name: nginx_billing_prod
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /mnt/ebs/certbot/conf:/etc/letsencrypt:ro
      - /mnt/ebs/certbot/www:/var/www/certbot:ro
      - /mnt/ebs/staticfiles:/var/www/static:ro
      - /mnt/ebs/media:/var/www/media:ro
    depends_on:
      - backend
    networks:
      - billing_network

  certbot:
    image: certbot/certbot
    container_name: certbot_billing
    volumes:
      - /mnt/ebs/certbot/conf:/etc/letsencrypt
      - /mnt/ebs/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - billing_network

networks:
  billing_network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/ebs/postgres_data






